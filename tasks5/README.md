# Task Manager (scaffold)

This repository contains a small scaffold for a CLI Task Manager feature.

See `specs/001-task-manager/spec.md` and `specs/001-task-manager/plan.md` for
the feature specification and plan.

Run the CLI locally:

```powershell
python -m taskmgr add --title "Buy milk"
python -m taskmgr list
```
