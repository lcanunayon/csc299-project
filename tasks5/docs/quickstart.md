# Quickstart: Task Manager (scaffold)

Create a task:

```powershell
python -m taskmgr add --title "Buy milk"
```

List tasks:

```powershell
python -m taskmgr list
```

Show a task:

```powershell
python -m taskmgr show 1
```
